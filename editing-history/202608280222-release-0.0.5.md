# Release 0.0.5 / 发布 0.0.5

- Releases the C-safe buffer FFI v1 migration for `run-command`.
- 发布 `run-command` 的 C 安全 buffer FFI v1 迁移。
- Requires Calcit 0.13.52 or newer and intentionally removes legacy Rust ABI exports.
- 要求 Calcit 0.13.52 或更新版本，并有意移除旧 Rust ABI 导出。
