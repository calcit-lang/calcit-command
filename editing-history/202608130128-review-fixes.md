# 2026-08-13 Calcit upgrade review fixes

- Keep `calcit.cirru` marked as generated for GitHub Linguist while preserving normal
  diffs, so the canonical project Snapshot remains reviewable.
- Document the Windows `.dll` native-library artifact alongside `.dylib` and `.so`.
- Remove unused namespace imports with `cr edit imports`, then validate the Snapshot
  using format, check-only, type coverage, and unresolved dynamic analysis.
